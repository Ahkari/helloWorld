/**
* This is the description for my class.(这里是俺的类的描述)
*
* @class MyClass
* @constructor
*/


/**
* My method description.  Like other pieces of your comment blocks, 
* this can span multiple lines.(和其他注释块一样，可以横跨很多行)
*
* @method methodName
* @param {String} foo Argument 1 (参数一)
* @param {Object} config A config object (一个配置对象)
* @param {String} config.name The name on the config object (配置对象的名字)
* @param {Function} config.callback A callback function on the config object (配置对象的回调函数)
* @param {Boolean} [extra=false] Do extra, optional work (可选)
* @return {Boolean} Returns true on success (成功返回true)
*/


/**
* My property description.  Like other pieces of your comment blocks, 
* this can span multiple lines.
* 
* @property propertyName
* @type {Object}
* @default "foo"
*/


/**
